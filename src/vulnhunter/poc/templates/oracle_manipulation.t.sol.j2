// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "forge-std/Test.sol";
import "../src/{{ contract_name }}.sol";

contract PoCOracleManipulation is Test {
    function setUp() public {
        // Setup
    }

    function test_oracle_manipulation_attack() public {
        // Template exploit for oracle manipulation. LLN should insert concrete steps.
    }
}
